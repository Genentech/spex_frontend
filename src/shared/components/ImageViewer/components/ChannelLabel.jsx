import styled from 'styled-components';

export default styled.div`
  display: flex;
  flex-wrap: nowrap;
  align-items: center;
  margin: 0 15px;
  font-size: 0.8em;
  word-break: break-word;

  min-width: 50px;
  max-width: 50px;

  overflow-x: hidden;
`;
